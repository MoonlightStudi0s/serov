# -*- coding: utf-8 -*-
"""Содержание отчёта по лабораторной работе № 3 «Настройка загрузчика GRUB2».

Структура — по шаблону отчёта из методических указаний и «Краткой выписке из
ГОСТ 7.32-2017»: титульный лист, РЕФЕРАТ, СОДЕРЖАНИЕ, разделы 1–6,
СПИСОК ИСПОЛЬЗОВАННЫХ ИСТОЧНИКОВ.

Выводы команд подставляются из ../outputs/N.txt без изменений (файлы 1–18),
скриншоты берутся из ../screenshots/N.jpg.

Типы блоков списка SECTIONS:
  ('h1c', 'РЕФЕРАТ', False) — структурный элемент: новый лист, по центру,
                              прописными; False — не включать в СОДЕРЖАНИЕ;
  ('h1',  '1 Название')     — раздел: полужирный, с абзацного отступа;
  ('h2',  '1.1 Название')   — подраздел: полужирный, разреженный на 3 пт;
  ('p',   'абзац')          — основной текст;
  ('pb',  'абзац')          — абзац полужирным (формулировка вопроса);
  ('list', ['пункт', ...])  — перечисление через тире;
  ('refs', ['источник', …]) — нумерованный список источников;
  ('fig', 'N.jpg', 'подпись')  — рисунок с подписью (нумерация автоматическая);
  ('code', 'текст вывода')  — вывод команды моноширинным шрифтом;
  ('table', {...})          — таблица;
  ('toc',)                  — вставка содержания;
  ('counts', 'текст {pages}') — абзац с автоматическим числом страниц.
"""

import os

BASE = os.path.dirname(os.path.abspath(__file__))
LAB = os.path.dirname(BASE)
OUTPUTS = os.path.join(LAB, 'outputs')


def out(number):
    """Вывод команды из ../outputs/<number>.txt (без завершающих переводов строки)."""
    with open(os.path.join(OUTPUTS, '%d.txt' % number), encoding='utf-8') as f:
        return f.read().rstrip('\n')


# --------------------------------------------------------------------- титульный лист
# 'gap' — дополнительный интервал после строки, пт; 'sz' — размер, пт.
TITLE = [
    {'t': 'IT-Колледж «Сириус»', 'b': True},
    {'t': 'Федеральная территория «Сириус»', 'sz': 12},
    {'t': '', 'gap': 24},
    {'t': 'ОТЧЁТ', 'b': True, 'sz': 18},
    {'t': 'о выполнении лабораторной работы № 3'},
    {'t': 'по дисциплине «Операционные системы и среды»'},
    {'t': '', 'gap': 12},
    {'t': 'Тема: «Настройка загрузчика GRUB2»', 'b': True},
    {'t': '', 'gap': 48},
    {'t': 'Выполнил: студент группы К0709-25/2'},
    {'t': 'Лернер Владислав Игоревич'},
    {'t': '', 'gap': 24},
    {'t': 'Принял: преподаватель'},
    {'t': 'Серов Валерий Александрович'},
    {'t': '', 'gap': 48},
    {'t': 'Дата выполнения: «20» сентября 2026 г.'},
    {'t': '', 'gap': 24},
    {'t': '2026'},
]

# --------------------------------------------------------------------------- реферат
KEYWORDS = ('GRUB2, ЗАГРУЗЧИК, ЯДРО LINUX, ПАРАМЕТРЫ ЯДРА, КОНФИГУРАЦИЯ GRUB, '
            '40_CUSTOM, ROSA LINUX, ВИРТУАЛЬНАЯ МАШИНА, КОМАНДНАЯ СТРОКА, '
            'ВОССТАНОВЛЕНИЕ СИСТЕМЫ')

ABSTRACT = [
    'Объектом работы является процесс загрузки операционной системы Linux и '
    'конфигурация загрузчика GRUB2.',
    'Цель работы — изучить процесс загрузки системы, научиться настраивать загрузчик '
    'GRUB2, работать с параметрами ядра и понимать их влияние на систему [1].',
    'Методологическую основу работы составляет натурный эксперимент на виртуальной '
    'машине с операционной системой «РОСА Фреш» 13.2: определение особенностей '
    'дистрибутива, исследование текущей конфигурации загрузчика, добавление '
    'дополнительного пункта меню и изменение параметров ядра с последующей проверкой '
    'результата после перезагрузки.',
    'В результате работы установлено, что в «РОСА Фреш» используется загрузчик GRUB2 '
    'с конфигурационным файлом /boot/grub2/grub.cfg и командой обновления '
    'grub2-mkconfig; в меню загрузки добавлен пункт «Linux (Recovery Mode)», '
    'загружающий ядро в однопользовательском режиме, а в параметры ядра добавлен '
    'параметр net.ifnames=0, применение которого подтверждено выводом /proc/cmdline '
    'после перезагрузки.',
]

# -------------------------------------------------------- список использованных источников
REFS = [
    'GNU GRUB Manual : руководство по загрузчику GRUB. — URL: '
    'https://www.gnu.org/software/grub/manual/grub/grub.html (дата обращения: 20.09.2026).',
    'grub-mkconfig(8), grub2-mkconfig(8) : справочные страницы (man-страницы) пакета '
    'grub2-tools операционной системы «РОСА Фреш» 13.2.',
    'The kernel’s command-line parameters // The Linux Kernel documentation. — URL: '
    'https://docs.kernel.org/admin-guide/kernel-parameters.html (дата обращения: 20.09.2026).',
    'proc(5) : справочная страница (файл /proc/cmdline) операционной системы Linux. — '
    'man 5 proc.',
    'lsblk(8) : справочная страница утилиты lsblk (вывод информации о блочных '
    'устройствах). — man 8 lsblk.',
    'Working with the GRUB 2 boot loader // Red Hat Enterprise Linux Documentation. — URL: '
    'https://docs.redhat.com/ (дата обращения: 20.09.2026).',
    'GRUB // ArchWiki. — URL: https://wiki.archlinux.org/title/GRUB '
    '(дата обращения: 20.09.2026).',
    'ОС «РОСА» : официальный сайт ООО «НТЦ ИТ РОСА». — URL: https://www.rosa.ru/ '
    '(дата обращения: 20.09.2026).',
]

# ----------------------------------------------------------------------------- текст ПЗ

SECTIONS = [
    # ------------------------------------------------------------------ РЕФЕРАТ
    ('h1c', 'РЕФЕРАТ', False),
    ('counts', 'Отчёт содержит: {pages} с., {figs} рис., {tables} табл., '
               '{sources} источников.'),
    ('p', 'Ключевые слова: ' + KEYWORDS + '.'),
] + [('p', text) for text in ABSTRACT] + [

    # --------------------------------------------------------------- СОДЕРЖАНИЕ
    ('h1c', 'СОДЕРЖАНИЕ', False),
    ('toc',),

    # ---------------------------------------------------------------- 1 ЦЕЛЬ РАБОТЫ
    ('h1', '1 Цель работы', 'break'),
    ('p', 'Целью лабораторной работы является изучение процесса загрузки операционной '
          'системы Linux, получение навыков настройки загрузчика GRUB2, работы с '
          'параметрами ядра и понимания их влияния на систему, а также умения '
          'самостоятельно определять особенности конкретного дистрибутива и применять '
          'корректные для него команды [1].'),
    ('p', 'Для достижения поставленной цели необходимо решить следующие задачи:'),
    ('list', [
        'определить дистрибутив, его версию и структуру загрузки системы;',
        'выяснить, какая команда обновления конфигурации GRUB доступна в системе, и '
        'где находится основной конфигурационный файл загрузчика;',
        'исследовать текущую конфигурацию загрузчика и состав скриптов каталога '
        '/etc/grub.d;',
        'добавить в меню загрузки дополнительный пункт (режим восстановления), '
        'проверить корректность генерации конфигурации и применить её;',
        'изменить параметры ядра, перезагрузить систему и убедиться в применении '
        'нового параметра.',
    ]),

    # ------------------------------------------------- 2 ТЕОРЕТИЧЕСКИЕ СВЕДЕНИЯ
    ('h1', '2 Краткие теоретические сведения'),
    ('p', 'GRUB2 (GRand Unified Bootloader версии 2) — основной загрузчик современных '
          'дистрибутивов Linux: он принимает управление от BIOS или UEFI, выводит меню '
          'выбора операционной системы, загружает в память ядро и образ initramfs и '
          'передаёт управление ядру [1]. Основной конфигурационный файл загрузчика '
          '(/boot/grub2/grub.cfg или /boot/grub/grub.cfg) не редактируется вручную: он '
          'формируется автоматически командой grub2-mkconfig по содержимому файла '
          'настроек /etc/default/grub и скриптов каталога /etc/grub.d [2].'),
    ('p', 'Собственные пункты меню удобно описывать в файле /etc/grub.d/40_custom: '
          'скрипты этого каталога выполняются по порядку возрастания имён, а их вывод '
          'попадает в итоговый grub.cfg. Перед применением изменения имеет смысл '
          'проверить командой grub2-mkconfig с выводом во временный файл, поскольку '
          'синтаксическая ошибка в конфигурации может сделать систему незагружаемой [6].'),
    ('p', 'Загрузчик передаёт ядру набор параметров командной строки, которые влияют на '
          'режим работы системы: ro — монтирование корневой файловой системы только для '
          'чтения на этапе загрузки, quiet и splash — сокращение объёма сообщений и '
          'графическая заставка, single — однопользовательский режим, net.ifnames=0 — '
          'имена сетевых интерфейсов в старом стиле (eth0 вместо предсказуемых имён) '
          '[3]. Параметры задаются переменными GRUB_CMDLINE_LINUX и '
          'GRUB_CMDLINE_LINUX_DEFAULT и попадают в файл /etc/default/grub, а '
          'фактически применённые параметры можно увидеть в файле /proc/cmdline — это '
          'командная строка, с которой было загружено работающее ядро [4].'),
    ('p', 'Разделы диска в конфигурации загрузчика принято указывать не по имени '
          'устройства (/dev/sda3), а по универсальному идентификатору UUID: он хранится '
          'в суперблоке файловой системы, не меняется при добавлении дисков и '
          'изменении порядка их обнаружения, а также однозначно определяет раздел с '
          'загрузочными файлами [5]. Набор команд и пути к конфигурации в разных '
          'дистрибутивах отличаются: в Debian, Ubuntu и Astra Linux применяются '
          'update-grub (grub-mkconfig) и файл /boot/grub/grub.cfg, а в Fedora, RHEL, '
          'CentOS и «РОСА» — grub2-mkconfig и файл /boot/grub2/grub.cfg [6, 7].'),

    # ------------------------------------------------------- 3 ИСПОЛЬЗУЕМОЕ ОКРУЖЕНИЕ
    ('h1', '3 Используемое окружение'),
    ('p', 'Работа выполнялась на виртуальной машине с операционной системой «РОСА Фреш» '
          '(ROSA Fresh Desktop) [8], развёрнутой в среде виртуализации VMware Workstation. '
          'Для изучения процесса загрузки использовался установочный носитель '
          'ROSA_13_PLASMA6_x86_64, подключённый к виртуальной машине в виде '
          'ISO-образа, а также установленная на виртуальный диск /dev/sda система. '
          'Сведения о дистрибутиве получены командой cat /etc/os-release, сведения о '
          'разметке диска — командой lsblk -f (раздел 4.1).'),
    ('p', 'Версия загруженного ядра определена из командной строки ядра '
          '(параметр BOOT_IMAGE вывода cat /proc/cmdline, рисунок 5) и '
          'подтверждена перечнем файлов каталога /boot, а сведения о дистрибутиве — '
          'выводом cat /etc/os-release. Основные характеристики окружения приведены '
          'в таблице 1.'),
    ('table', {
        'caption': 'Таблица 1 – Параметры используемого окружения',
        'head': ['Параметр', 'Значение'],
        'widths': [6.0, 10.5],
        'rows': [
            ['Дистрибутив', 'ROSA Fresh Desktop («РОСА Фреш»), ID: rosa'],
            ['Версия дистрибутива', '13.2 (VERSION_ID=13, ROSA_OS_PLATFORM=rosa13)'],
            ['Версия загруженного ядра', '6.12.74-generic-2rosa13-x86_64'],
            ['Тип виртуальной машины', 'VMware Workstation (гостевая ОС «РОСА Фреш»)'],
            ['Имя узла (hostname)', 'rosa-me7kar'],
            ['Учётная запись пользователя', 'lerner-v'],
            ['Разметка диска', 'GPT, разделы /dev/sda1 – /dev/sda4'],
            ['Команда обновления GRUB', '/usr/sbin/grub2-mkconfig'],
            ['Основной конфигурационный файл', '/boot/grub2/grub.cfg'],
            ['Файл настроек', '/etc/default/grub'],
        ],
    }),
    ('p', 'Виртуальная машина оснащена виртуальным диском /dev/sda, разделённым на '
          'служебный раздел biosboot (/dev/sda1), раздел подкачки (/dev/sda2) и два '
          'раздела с файловой системой ext4 — корневой (/dev/sda3) и пользовательский '
          '(/dev/sda4, смонтирован в /home).'),

    # ------------------------------------------------------- 4 ХОД ВЫПОЛНЕНИЯ
    ('h1', '4 Ход выполнения работы'),
    ('p', 'Все команды вводились в терминале Konsole от имени пользователя lerner-v; '
          'команды, изменяющие настройки загрузчика, выполнялись с правами '
          'суперпользователя через sudo. Выводы команд фиксировались по ходу работы: '
          'в отчёте они приведены как фрагменты терминала моноширинным шрифтом, а '
          'внешний вид окон показан на рисунках.'),

    # ------------------------------------------------------------- задание 0
    ('h2', '4.1 Задание 0. Определение дистрибутива и структуры загрузки'),
    ('p', 'Работа началась с определения дистрибутива. Команда cat /etc/os-release '
          'выводит сведения о системе из файла /etc/os-release: в выводе указано имя '
          '«ROSA Fresh Desktop», идентификатор rosa, версия 13.2, платформа rosa13 и '
          'домашняя страница проекта rosa.ru (рисунок 1).'),
    ('fig', '1.jpg', 'Вывод команды cat /etc/os-release'),
    ('code', out(1)),
    ('p', 'Затем было выяснено, какая команда обновления конфигурации загрузчика '
          'доступна в системе. Команда which показывает путь к исполняемому файлу и '
          'сообщает об отсутствии остальных: в «РОСА Фреш» найдена только '
          '/usr/sbin/grub2-mkconfig, а команд update-grub и grub-mkconfig в системе '
          'нет (рисунок 2).'),
    ('fig', '2.jpg', 'Проверка доступных команд обновления GRUB'),
    ('code', out(2)),
    ('p', 'Далее был определён реальный путь к основному конфигурационному файлу '
          'загрузчика. Из двух проверенных путей существует только второй: файл '
          '/boot/grub2/grub.cfg доступен для чтения лишь суперпользователю '
          '(права -rw-------), его размер — 9555 байт (рисунок 3).'),
    ('fig', '3.jpg', 'Расположение конфигурационного файла GRUB'),
    ('code', out(3)),
    ('p', 'Разметка диска и точки монтирования были получены командой lsblk -f '
          '(рисунок 4): на диске /dev/sda созданы служебный раздел /dev/sda1 без '
          'файловой системы, раздел подкачки /dev/sda2, корневой раздел /dev/sda3 с '
          'файловой системой ext4 (смонтирован в /) и раздел /dev/sda4, смонтированный '
          'в /home. Здесь же видны UUID разделов, а также подключённый установочный '
          'образ sr0 с меткой ROSA_13_PLASMA6_x86_64.'),
    ('fig', '4.jpg', 'Разметка диска и точки монтирования (lsblk -f)'),
    ('code', out(4)),
    ('p', 'Разделы виртуальной машины и их идентификаторы приведены в таблице 2.'),
    ('table', {
        'caption': 'Таблица 2 – Разделы диска виртуальной машины',
        'head': ['Устройство', 'Файловая система', 'UUID', 'Точка монтирования'],
        'widths': [2.6, 3.4, 7.0, 3.5],
        'rows': [
            ['/dev/sda1', 'нет (biosboot)', '—', '—'],
            ['/dev/sda2', 'swap', 'a1a00e8a-a6ef-464f-a924-c1b3f504194c', '[SWAP]'],
            ['/dev/sda3', 'ext4', '0f3daa60-8569-4981-a4f9-342b6d6c16ce', '/'],
            ['/dev/sda4', 'ext4', 'd74c47f7-3c42-47d2-aeb8-36c6a20f9adc', '/home'],
        ],
    }),
    ('p', 'Отдельный раздел для каталога /boot в системе не создавался: в выводе lsblk '
          'точка монтирования /boot отсутствует, поэтому файлы ядра и initramfs '
          'расположены на корневом разделе /dev/sda3. Это важно учитывать при '
          'составлении записи menuentry: и раздел для поиска файлов ядра '
          '(search --fs-uuid), и корневой раздел (root=UUID=…) в данном случае имеют '
          'один и тот же идентификатор 0f3daa60-8569-4981-a4f9-342b6d6c16ce.'),
    ('p', 'Завершающим шагом задания 0 было определение параметров, с которыми загружено '
          'работающее ядро. Команда cat /proc/cmdline выводит командную строку ядра '
          '(рисунок 5): в ней указаны путь к загруженному образу ядра, идентификатор '
          'корневого раздела, режим монтирования ro, параметры quiet, splash=silent, '
          'selinux=0, языковые настройки и раздел подкачки для восстановления после '
          'гибернации.'),
    ('fig', '5.jpg', 'Параметры, с которыми загружено ядро (cat /proc/cmdline)'),
    ('code', out(5)),
    ('p', 'Таким образом, для дальнейшей работы получены следующие исходные данные: '
          'дистрибутив «РОСА Фреш» 13.2; доступная команда обновления загрузчика — '
          '/usr/sbin/grub2-mkconfig; основной конфигурационный файл — '
          '/boot/grub2/grub.cfg; раздел с каталогом /boot — /dev/sda3 с UUID '
          '0f3daa60-8569-4981-a4f9-342b6d6c16ce (отдельного раздела /boot нет, поэтому '
          'его UUID совпадает с UUID корневого раздела); корневой раздел — '
          'root=UUID=0f3daa60-8569-4981-a4f9-342b6d6c16ce; версия загруженного ядра — '
          '6.12.74-generic-2rosa13-x86_64 (имя файла ядра — '
          '/boot/vmlinuz-6.12.74-generic-2rosa13-x86_64, имя образа initramfs — '
          '/boot/initrd-6.12.74-generic-2rosa13-x86_64.img).'),

    # ------------------------------------------------------------- задание 1
    ('h2', '4.2 Задание 1. Исследование текущей конфигурации GRUB'),
    ('p', 'Текущие настройки загрузчика хранятся в файле /etc/default/grub. Его '
          'содержимое, выведенное командой cat (рисунок 6), приведено ниже.'),
    ('fig', '6.jpg', 'Содержимое файла /etc/default/grub'),
    ('code', out(6)),
    ('p', 'Наибольший интерес представляет переменная GRUB_CMDLINE_LINUX_DEFAULT, '
          'задающая параметры ядра для обычной загрузки: в исходном состоянии она '
          'равна «splash=silent logo.nologo quiet». Кроме неё в файле заданы таймаут '
          'меню (GRUB_TIMEOUT="4"), дистрибутив (GRUB_DISTRIBUTOR="ROSA"), тема '
          'оформления и фон меню, переменная GRUB_DISABLE_RECOVERY=true (пункт '
          'восстановления, создаваемый системой, отключён), а также '
          'GRUB_ENABLE_BLSCFG=true — признак того, что пункты меню формируются по '
          'спецификации Boot Loader Specification.'),
    ('p', 'Дополнительно был изучен сам сценарий генерации конфигурации: команда '
          'sudo cat /usr/sbin/grub2-mkconfig выводит его текст, из которого видно, что '
          'сценарий собирает данные о загрузочных образах и выполняет по очереди '
          'исполняемые файлы каталога /etc/grub.d (рисунок 7). Именно поэтому '
          'пользовательские пункты меню принято добавлять в файл 40_custom этого '
          'каталога, а не править итоговый grub.cfg.'),
    ('fig', '7.jpg', 'Первые 50 строк сценария /usr/sbin/grub2-mkconfig'),
    ('code', out(7)),
    ('p', 'Текущие параметры ядра были повторно зафиксированы командой '
          'cat /proc/cmdline (рисунок 8): параметры splash=silent, logo.nologo и quiet '
          'из GRUB_CMDLINE_LINUX_DEFAULT действительно присутствуют в командной строке '
          'загруженного ядра, то есть файл /etc/default/grub и итоговая конфигурация '
          'согласованы между собой.'),
    ('fig', '8.jpg', 'Текущие параметры ядра (cat /proc/cmdline)'),
    ('code', out(8)),
    ('p', 'Состав каталога /etc/grub.d был получен командой ls -la (рисунок 9). '
          'Сценарии выполняются в порядке имён: 00_header формирует общие настройки '
          'меню, 10_linux — пункты для установленных ядер, 20_memtest86 — проверку '
          'памяти, 30_os-prober — поиск других операционных систем, 35_fwupd — '
          'загрузку обновлений микропрограмм, а 40_custom и 41_custom предназначены '
          'для пунктов, добавленных пользователем. Все сценарии, включая 40_custom, '
          'имеют право на исполнение.'),
    ('fig', '9.jpg', 'Сценарии генерации конфигурации в каталоге /etc/grub.d'),
    ('code', out(9)),

    # ------------------------------------------------------------- задание 2
    ('h2', '4.3 Задание 2. Добавление нового пункта меню в GRUB'),
    ('p', 'Перед изменением настроек загрузчика была создана резервная копия файла '
          '/etc/default/grub, после чего открыт для редактирования файл '
          '/etc/grub.d/40_custom, а самому файлу выданы права на исполнение (рисунок 10).'),
    ('fig', '10.jpg', 'Резервное копирование настроек и создание файла 40_custom'),
    ('code', out(10)),
    ('p', 'В файл /etc/grub.d/40_custom была добавлена запись menuentry «Linux (Recovery '
          'Mode)», загружающая ядро в однопользовательском режиме, — она позволяет '
          'загрузиться для восстановления системы, например, при необходимости '
          'сбросить пароль суперпользователя или исправить повреждённую конфигурацию '
          '(рисунок 11).'),
    ('fig', '11.jpg', 'Содержимое файла /etc/grub.d/40_custom в редакторе nano'),
    ('code', out(11)),
    ('p', 'При составлении записи вместо шаблонных обозначений '
          'UUID_РАЗДЕЛА_BOOT, UUID_КОРНЯ и ВЕРСИЯ_ЯДРА подставлены реальные значения, '
          'полученные в задании 0: раздел для поиска файлов ядра — корневой раздел '
          '/dev/sda3 (отдельного раздела /boot в системе нет), корневой раздел — тот '
          'же /dev/sda3, а версия ядра взята из имени файла в каталоге /boot. '
          'Использованные значения приведены в таблице 3.'),
    ('table', {
        'caption': 'Таблица 3 – Значения, подставленные в запись menuentry',
        'head': ['Обозначение в шаблоне', 'Подставленное значение'],
        'widths': [6.0, 10.5],
        'rows': [
            ['UUID_РАЗДЕЛА_BOOT (раздел с /boot)',
             '0f3daa60-8569-4981-a4f9-342b6d6c16ce (/dev/sda3)'],
            ['UUID_КОРНЯ (параметр root=)',
             '0f3daa60-8569-4981-a4f9-342b6d6c16ce (/dev/sda3)'],
            ['ВЕРСИЯ_ЯДРА (имя файла в /boot)',
             '6.12.74-generic-2rosa13-x86_64'],
            ['Файл ядра', '/boot/vmlinuz-6.12.74-generic-2rosa13-x86_64'],
            ['Файл initramfs', '/boot/initrd-6.12.74-generic-2rosa13-x86_64.img'],
        ],
    }),
    ('p', 'Версия ядра записана в конфигурации явно: загрузчик не является командной '
          'оболочкой и не выполняет подстановку команд вида $(uname -r). Кроме того, '
          'имя образа initramfs в дистрибутивах семейства Red Hat, к которому '
          'относится «РОСА», имеет вид /initrd-ВЕРСИЯ.img, а не /initrd.img-ВЕРСИЯ, '
          'как в Debian и Ubuntu [6].'),
    ('p', 'Чтобы не повредить загрузку системы, первая генерация конфигурации была '
          'выполнена в проверочный файл /tmp/grub.test. Стандартный вывод команды '
          'grub2-mkconfig показывает найденные образы ядра и initramfs, тему и фон '
          'меню, предупреждение о запуске os-prober и завершается словом «завершено», '
          'то есть синтаксических ошибок в записи нет (рисунок 12).'),
    ('fig', '12.jpg', 'Проверочная генерация конфигурации во временный файл'),
    ('code', out(12)),
    ('p', 'После успешной проверки конфигурация была сгенерирована в штатный файл '
          '/boot/grub2/grub.cfg командой grub2-mkconfig -o /boot/grub2/grub.cfg '
          '(рисунок 13). Это команда дистрибутивов семейства Red Hat: в Debian, '
          'Ubuntu и Astra Linux аналогичное действие выполняют командами update-grub '
          'или grub-mkconfig с путём /boot/grub/grub.cfg [2, 6].'),
    ('fig', '13.jpg', 'Применение новой конфигурации загрузчика'),
    ('code', out(13)),
    ('p', 'Наличие новой записи в итоговом файле проверено командой grep -A8 '
          '(рисунок 14): запись «Linux (Recovery Mode)» присутствует в конфигурации '
          'вместе со всеми восемью строками описания — загрузкой шрифтов gzio, модулей '
          'part_gpt и ext2, поиском раздела по UUID, загрузкой ядра с параметрами '
          'root=UUID=… ro single и загрузкой образа initrd.'),
    ('fig', '14.jpg', 'Проверка наличия новой записи в файле grub.cfg'),
    ('code', out(14)),
    ('p', 'Таким образом, в меню загрузки появился дополнительный пункт, при выборе '
          'которого ядро загружается с параметром single, то есть в '
          'однопользовательском режиме — без запуска большинства служб и графической '
          'среды, что и требуется для восстановительных работ [3].'),

    # ------------------------------------------------------------- задание 3
    ('h2', '4.4 Задание 3. Изменение параметров ядра'),
    ('p', 'Завершающим заданием было изменение параметров, передаваемых ядру при обычной '
          'загрузке. Для этого файл настроек /etc/default/grub был открыт в редакторе '
          'nano (рисунок 15), а строка GRUB_CMDLINE_LINUX_DEFAULT была дополнена '
          'параметром net.ifnames=0 (рисунок 16).'),
    ('fig', '15.jpg', 'Открытие файла настроек /etc/default/grub в редакторе nano'),
    ('code', out(15)),
    ('fig', '16.jpg', 'Изменение строки GRUB_CMDLINE_LINUX_DEFAULT'),
    ('p', 'Состояние строки GRUB_CMDLINE_LINUX_DEFAULT до и после правки приведено '
          'ниже (все остальные переменные файла остались без изменений).'),
    ('code', out(16)),
    ('p', 'После сохранения файла конфигурация загрузчика была сгенерирована заново '
          'командой grub2-mkconfig -o /boot/grub2/grub.cfg (рисунок 17): команда '
          'вновь нашла образы ядра и initramfs и завершилась словом «завершено».'),
    ('fig', '17.jpg', 'Обновление конфигурации после изменения параметров ядра'),
    ('code', out(17)),
    ('p', 'Затем система была перезагружена командой sudo reboot, и после загрузки '
          'параметры ядра проверены повторно командой cat /proc/cmdline (рисунок 18). '
          'В командной строке загруженного ядра появился параметр net.ifnames=0, '
          'который отсутствовал там до изменения настроек.'),
    ('fig', '18.jpg', 'Параметры ядра после перезагрузки'),
    ('code', out(18)),
    ('p', 'Сравнение выводов cat /proc/cmdline до и после перезагрузки показывает, что '
          'добавленный параметр применился: если в исходной командной строке '
          '(рисунок 5) параметра net.ifnames=0 не было, то после повторной генерации '
          'конфигурации и перезагрузки он присутствует в выводе (рисунок 18). '
          'Следовательно, изменение файла /etc/default/grub и последующая генерация '
          'grub.cfg выполнены верно.'),

    # ------------------------------------------------------ 5 КОНТРОЛЬНЫЕ ВОПРОСЫ
    ('h1', '5 Ответы на контрольные вопросы'),
    ('pb', 'Вопрос 1. Для чего нужен параметр single при загрузке ядра?'),
    ('p', 'Параметр single сообщает ядру и системе инициализации, что нужно загрузиться '
          'в однопользовательском режиме (аналог systemd-цели rescue.target): '
          'монтируется корневая файловая система, запускается минимальный набор служб '
          'и не стартует графическая среда. Этот режим применяется для '
          'восстановительных работ — сброса забытого пароля суперпользователя, '
          'исправления повреждённых конфигурационных файлов, проверки и починки '
          'файловой системы. В таком режиме система доступна только локально от имени '
          'администратора, поэтому он не предназначен для обычной работы [3].'),
    ('pb', 'Вопрос 2. Чем отличаются команды обновления GRUB в Debian-подобных и '
           'RPM-подобных дистрибутивах? Почему нельзя слепо копировать команды из '
           'инструкции для другой системы?'),
    ('p', 'В Debian-подобных дистрибутивах (Debian, Ubuntu, Astra Linux, Mint) '
          'используется команда update-grub — обёртка над grub-mkconfig, а '
          'конфигурация размещается в файле /boot/grub/grub.cfg. В RPM-подобных '
          'системах (Fedora, RHEL, CentOS, «РОСА», openSUSE) применяется команда '
          'grub2-mkconfig, а основной файл находится по пути /boot/grub2/grub.cfg. '
          'Слепое копирование команд опасно тем, что необходимой команды в системе '
          'может просто не быть, а указание чужого пути приведёт к созданию '
          'конфигурации «не в том месте»: загрузчик продолжит читать старый файл, либо '
          'будет испорчена действующая конфигурация и система перестанет загружаться '
          '[2, 6].'),
    ('pb', 'Вопрос 3. Почему не рекомендуется редактировать файл /boot/grub/grub.cfg '
           '(или /boot/grub2/grub.cfg) напрямую?'),
    ('p', 'Этот файл является результатом работы генератора: он полностью '
          'перезаписывается при каждом выполнении grub2-mkconfig, поэтому внесённые '
          'вручную правки будут потеряны при первом же обновлении ядра или изменении '
          'настроек. Кроме того, файл имеет сложную структуру (проверки оборудования, '
          'загрузка модулей, подстановка переменных), и синтаксическая ошибка в нём '
          'делает систему незагружаемой. Правильный способ — изменять /etc/default/grub '
          'или добавлять свои пункты через скрипты каталога /etc/grub.d (например, '
          '40_custom), после чего генерировать конфигурацию командой grub2-mkconfig '
          '[1, 2].'),
    ('pb', 'Вопрос 4. Почему в записи menuentry нельзя использовать $(uname -r)? '
           'Как правильно указать версию ядра?'),
    ('p', 'Конструкция $(uname -r) является подстановкой команд оболочки, а GRUB '
          'разбирает свой конфигурационный файл собственным интерпретатором, который '
          'не умеет выполнять программы и обрабатывать вывод команд. Поэтому версия '
          'ядра указывается в записи явно, как имя файла образа: '
          'linux /vmlinuz-6.12.74-generic-2rosa13-x86_64 и initrd '
          '/initrd-6.12.74-generic-2rosa13-x86_64.img. Актуальное имя файла можно '
          'посмотреть командой ls /boot/ или взять из вывода cat /proc/cmdline; при '
          'установке нового ядра жёстко прописанное имя придётся обновить вручную '
          '[1, 6].'),
    ('pb', 'Вопрос 5. Как восстановить исходную конфигурацию GRUB в случае ошибки?'),
    ('p', 'Если ошибка обнаружена до применения конфигурации, достаточно вернуть '
          'исходное содержимое файла настроек из резервной копии '
          '(sudo cp /etc/default/grub.backup /etc/default/grub) и заново сгенерировать '
          'конфигурацию командой grub2-mkconfig, предварительно проверив её вывод во '
          'временный файл. Если система уже не загружается, применяют загрузку с '
          'установочного носителя или из более старого пункта меню, монтируют '
          'корневой раздел, удаляют ошибочный скрипт из /etc/grub.d и повторяют '
          'генерацию конфигурации в режиме chroot. В крайнем случае помогает ручной '
          'ввод команд загрузки ядра в командной строке GRUB по нажатию клавиши «c» '
          '[1, 6].'),
    ('pb', 'Вопрос 6. Как узнать UUID раздела и почему в GRUB предпочтительнее '
           'использовать UUID, а не имена вида /dev/sda2?'),
    ('p', 'UUID раздела и файловой системы можно узнать командами lsblk -f, blkid или '
          'из вывода cat /proc/cmdline для корневого раздела. В конфигурации GRUB '
          'предпочтительнее указывать именно UUID, потому что он записан в суперблок '
          'файловой системы и не зависит от порядка обнаружения устройств: при '
          'добавлении диска, подключении нового накопителя или изменении настроек '
          'виртуальной машины имена /dev/sda, /dev/sdb могут поменяться, и загрузчик '
          'обратится не к тому разделу. UUID же остаётся неизменным, что делает '
          'загрузку предсказуемой [5].'),

    # ------------------------------------------------------------------ 6 ВЫВОД
    ('h1', '6 Вывод по работе'),
    ('p', 'В ходе лабораторной работы изучен процесс загрузки операционной системы '
          'Linux и получены практические навыки настройки загрузчика GRUB2. Было '
          'установлено, что работа выполнялась в дистрибутиве «РОСА Фреш» 13.2 с '
          'ядром 6.12.74-generic-2rosa13-x86_64, относящемся к семейству Red Hat: '
          'в системе доступна только команда /usr/sbin/grub2-mkconfig, а конфигурация '
          'загрузчика находится в файле /boot/grub2/grub.cfg. В меню загрузки '
          'добавлен дополнительный пункт «Linux (Recovery Mode)», загружающий систему '
          'в однопользовательском режиме, а в параметры ядра добавлен параметр '
          'net.ifnames=0, применение которого подтверждено выводом /proc/cmdline '
          'после перезагрузки. Все изменения выполнялись через файл /etc/default/grub '
          'и скрипт /etc/grub.d/40_custom с обязательной предварительной проверкой '
          'генерации во временный файл.'),
    ('p', 'Основные трудности были связаны с особенностями конкретного дистрибутива. '
          'Во-первых, привычные по инструкциям команды update-grub и grub-mkconfig в '
          '«РОСА» отсутствуют, а путь к конфигурации отличается от '
          'Debian-подобных систем, поэтому в отчёте важно было опираться на '
          'фактический вывод команды which. Во-вторых, в системе не создан отдельный '
          'раздел /boot, из-за чего UUID раздела с загрузочными файлами совпадает с '
          'UUID корневого раздела — это выяснилось только при анализе вывода lsblk -f. '
          'В-третьих, имя образа initramfs в дистрибутивах семейства Red Hat '
          'отличается от привычного для Debian, поэтому имена файлов пришлось '
          'проверять в каталоге /boot, а не подставлять по образцу инструкции.'),
    ('p', 'Сравнение двух семейств дистрибутивов, рассмотренных в работе, удобно '
          'представить в виде таблицы 4: в Astra Linux и других '
          'Debian-подобных системах конфигурация обновляется командой update-grub '
          '(либо grub-mkconfig) и хранится в файле /boot/grub/grub.cfg, тогда как '
          'в «РОСА», как и в других RPM-подобных системах, используется команда '
          'grub2-mkconfig и файл /boot/grub2/grub.cfg. Именно поэтому команды из '
          'инструкции для другой системы нельзя применять не проверив, какая '
          'команда и какой путь к конфигурации существуют в конкретной системе '
          '[2, 6].'),
    ('table', {
        'caption': 'Таблица 4 – Отличия в настройке GRUB у разных семейств дистрибутивов',
        'head': ['Дистрибутивы', 'Команда обновления', 'Путь к конфигурации'],
        'widths': [5.5, 5.5, 5.5],
        'rows': [
            ['Debian, Ubuntu, Astra Linux, Mint', 'update-grub, grub-mkconfig',
             '/boot/grub/grub.cfg'],
            ['Fedora, RHEL, CentOS, «РОСА», openSUSE', 'grub2-mkconfig',
             '/boot/grub2/grub.cfg'],
        ],
    }),
    ('p', 'Полученные навыки позволяют осознанно относиться к изменениям системы '
          'загрузки: перед правкой конфигурации создавать резервные копии, проверять '
          'результат генерации во временном файле, применять изменения под правами '
          'суперпользователя и обязательно контролировать результат после '
          'перезагрузки. Сравнение вывода cat /proc/cmdline до и после изменения '
          'настроек показало, что параметры командной строки ядра — это простой и '
          'надёжный инструмент управления поведением системы при загрузке [1, 3].'),

    # ------------------------------------------------------- СПИСОК ИСТОЧНИКОВ
    ('h1c', 'СПИСОК ИСПОЛЬЗОВАННЫХ ИСТОЧНИКОВ'),
    ('refs', REFS),
]
